//for-in
const person = {
    name : 'bhargav',
    age : 20
}
for(let key in person)
console.log(key, person[key]);

//for-of

const colors = ['pink','red','white'];
for(let color of colors)
console.log(color);

