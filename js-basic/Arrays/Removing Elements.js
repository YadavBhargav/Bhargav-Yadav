const numbers = [1, 2, 3, 4];

// End (Removing value from end of value)
const last = numbers.pop();

//Beginning (Removing value from begin value)
const fisrt = numbers.shift();

//Middle (Removing value from middle value)
const middle = numbers.splice(2, 1);

console.log(numbers);